<!DOCTYPE html>
<html>
<head>
    <title>Créer un type d'élément</title>
</head>
<body>
    <h1>Créer un nouveau type d'élément</h1>

    {% if app.request.query.get('success') %}
        <p style="color: green;">Type d'élément créé avec succès !</p>
    {% elseif app.request.query.get('error') %}
        <p style="color: red;">Erreur : Le nom ne peut pas être vide.</p>
    {% endif %}

    <form method="POST" action="/element/createType">
        <label>Nom du type : <input type="text" name="nom" required></label>
        <button type="submit">Créer</button>
    </form>

    <a href="/">Retour à l'accueil</a>
</body>
</html>
